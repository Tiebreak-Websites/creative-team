# Platform backend dependencies. The banner engine itself is Python stdlib-only;
# these are just the web layer + the AI-assist client.
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30
anthropic>=0.39
pyjwt>=2.8
passlib[bcrypt]>=1.7
# passlib 1.7.x's bcrypt backend is incompatible with bcrypt 5.x
# (raises on import: "password cannot be longer than 72 bytes"). Pin <4.1.
bcrypt<4.1

# Pillow — exact-pixel reshape (cover-crop) for display-ad export sizes, and
# raster brand-logo compositing onto finished banners.
Pillow>=10

# cairosvg — rasterize SVG brand logos so they can be composited as pixels
# (needs the native libcairo2, installed in the Dockerfile). The import in
# banner_engine/logo_overlay.py is guarded, so a missing native lib degrades to
# "skip the overlay" instead of crashing the app.
cairosvg>=2.7

# Multipart form parsing — POST /references uploads style reference images.
python-multipart>=0.0.9
